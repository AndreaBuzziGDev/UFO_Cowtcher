using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StructureEffectHelper
{
    //ALL STATIC CODE

    //
    public static bool HasTerrorEffect()
    {
        /*
        foreach (SAAbstract sa in GetPlayerAlterations())
        {
            if (sa.Type == SAAbstract.EBuffType.SpeedBoost) return true;
        }

        return false;
        */
    }

    public static bool HasSlowDownEffect()
    {
        /*
        foreach (SAAbstract sa in GetPlayerAlterations())
        {
            if (sa.Type == SAAbstract.EBuffType.SpeedBoost) return true;
        }

        return false;
        */
    }

}
